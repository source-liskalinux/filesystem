export PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

if [ -t 1 ] && [ -n "$PS1" ] && [ -x /usr/bin/zsh ]; then
    if [ -z "$ZSH_VERSION" ]; then
        export SHELL=/usr/bin/zsh
        exec /usr/bin/zsh
    fi
fi
